package lib std;

namespace Io;
namespace Filesystem;

using Fs = Filesystem;

namespace Fmt;
namespace Strings;
namespace Math;
namespace Containers;

if config::platform == .Win32
    namespace win32;
else
    namespace unix;

using Fmt::{print, format, println};

